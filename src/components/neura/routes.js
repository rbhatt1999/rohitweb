// Routes that belong to the NEURA product site. On these we hide the personal
// portfolio nav/footer and render the NEURA header/footer instead.
export const NEURA_ROUTES = ['/', '/features', '/pricing', '/privacy', '/terms']

export const NEURA_EMAIL = 'rohit@rohitcodes.com'
